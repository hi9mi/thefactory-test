# thefactory-test

## How to run locally

Clone repo:

```shell
git clone https://github.com/hi9mi/thefactory-test.git
```

Then install dependencies:

```shell
pnpm i
```

And then run locally:

```shell
pnpm dev --host
```

## Deployed app

WIP
